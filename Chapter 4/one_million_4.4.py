#make a list of the number from one to one million, then use a for lopp to print the numbers.
#(if the output is taking too long, stop it by pressiong the CTRL-C or by closing the output window)

one_million = list(range(1, 1000001))
print(one_million)